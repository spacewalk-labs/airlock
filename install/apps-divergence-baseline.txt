# Known apps/ drift between this repository and the published apps repository.
# Measured 2026-08-17 against work origin/main 82aeaab and apps origin/main 407bd08.
# kind is recorded for humans. The gate keys on the path: a new path that
# splits is red even if the count stays the same.
# Do not replace this list with a number.
#
# Two expected batches, both until D-APPS-DUP cutover:
# 1) 2026-08-16 live drift: work-ahead installer files (dev-monitor/devterm).
#    Work remains the live consumer of apps/. The public copy was behind.
# 2) 2026-08-17 PUBLIC_APP_PARITY #1: public-repo-only tests, migrators,
#    state helpers, fonts, and patches landed on airlock-apps. Work does not
#    get a second copy of those files. Existing files PARITY edited on the
#    apps side stay content-diff. Copying them into work would undo the split.
# 3) 2026-08-22 -> 2026-08-25: apps/learning — published. The owner ruled every
#    app public ("다 공개앱으로 일단넣어", 2026-08-25) and airlock-apps#12 merged
#    (7d883903), so this batch emptied exactly as it said it would. What is left
#    is the batch (2) mirror image: the published app carries its own lifecycle
#    test on the apps side.
only-apps	apps/learning/tests/lifecycle.sh
#    Three of learning's shells differ by MODE only: 100644 here, 100755 on the
#    apps side, where the publish gate requires the executable bit. Every other
#    app is 100755 on both sides, so work is the odd one out and the honest fix
#    would be to flip it here — but cutline-guard refuses exactly that
#    ("PolicyError: mode 100644->100755 is not allowed", measured 2026-08-25).
#    Two rules therefore disagree, and this file is not the place to overrule
#    either. Recorded as content drift, with the reason, so the next reader does
#    not mistake it for a stale entry that should have healed.
content	apps/learning/deactivate.sh
content	apps/learning/install.sh
content	apps/learning/smoke.sh
only-work	apps/devterm/test-accounts-js.mjs
# 3) 2026-08-18 P3 severity/routing: one work-ahead backend module. devmon_email.py is the
#    email lane's transport — a real source file of a mirrored app, which the public copy
#    gets on the next publish sync. That is batch 1's shape ("work remains the live consumer
#    of apps/, the public copy was behind"), not a new exception.
#    P3's other new file, the routing-policy contract test, is deliberately NOT here: it
#    reads a page and a backend module by path and can never be mirrored, so it lives in
#    install/ instead. Registering it would have widened this guard in the direction it
#    exists to close — the same call P2 made for test-inbox-state.mjs.
# 4) 2026-08-18 P4 owner/roster: one more work-ahead backend module, same shape as
#    devmon_email.py above. devmon_roster.py resolves a card's owner against the roster
#    snapshot for the Slack/email/console axes devmon_messages.py, devmon_slack.py and
#    devmon_email.py already read — it belongs in the public mirror and will land there on
#    the next publish sync. It is only-work today because that sync has not run since this
#    file was added, not because it is work-only by design.
# 5) 2026-08-21 provider-subagent selective delivery: a new paseo patch GROUP, split the
#    same way batch 1 describes — work is the live consumer and the public copy is behind.
#    The server filter trio (.mjs/.patch/.test.mjs) mirrors the shape airlock-apps already
#    uses for codex-strip-ambient-openai-key below, only on the other side: a patch that
#    exists on one repo and lands on the other at the next publish sync. It is only-work
#    today because that sync has not run, not because it is work-only by design.
only-work	apps/paseo/patches/schedule-busy-pending-delivery.mjs
only-work	apps/paseo/patches/schedule-busy-pending-delivery.patch
only-work	apps/paseo/patches/schedule-busy-pending-delivery.test.mjs
only-work	apps/paseo/patches/schedule-pending-delivery-schema.mjs
only-work	apps/paseo/patches/schedule-pending-delivery-schema.patch
only-work	apps/paseo/patches/finish-notification-queue.mjs
only-work	apps/paseo/patches/finish-notification-queue.patch
only-work	apps/paseo/patches/finish-notification-queue.test.mjs
only-work	apps/paseo/patches/claude-model-fable51.mjs
only-work	apps/paseo/patches/provider-subagent-stream-filter.mjs
only-work	apps/paseo/patches/provider-subagent-stream-filter.patch
only-work	apps/paseo/patches/provider-subagent-stream-filter.test.mjs
only-work	apps/paseo/browse-host/bin/patch-web-ui.test.js
# 6) 2026-08-21 P6 dev-monitor console replacement: hardening and migration helpers are
#    work-ahead package files, while the three existing backend/test paths are expected
#    content splits until the next public mirror synchronization.
only-work	apps/dev-monitor/install-spool-hardening.sh
only-work	apps/dev-monitor/migration-lifecycle.sh
#    2026-09-12 PR #450: the deferred-activation record tool lands beside the
#    migration lifecycle it serves (batch 1 kind — work-ahead dev-monitor installer file).
only-work	apps/dev-monitor/activation-record.py
only-work	apps/dev-monitor/test-migrate-legacy-state.py
only-apps	apps/code-server/bin/migrate-legacy-state
only-apps	apps/code-server/tests/lifecycle.sh
only-apps	apps/code-server/tests/parity.sh
only-apps	apps/dev-monitor/tests/lifecycle.sh
only-apps	apps/dev-monitor/tests/parity.sh
only-apps	apps/devterm/NOTICE
only-apps	apps/devterm/tests/lifecycle.sh
only-apps	apps/devterm/tests/parity.sh
only-apps	apps/devterm/tests/parity_test.py
# 10) 2026-08-24 platform-owned credentials P2a: the work tree deliberately removes
#    devterm's bundled account binaries after installing compatibility shims that target
#    the platform copies. airlock-apps still has the two stale files until the next
#    publish sync, so both are only-apps in the intended post-P2a tree. claude-status was
#    previously a content split; changing its kind is part of the ownership move.
only-apps	apps/devterm/bin/claude-status
only-apps	apps/devterm/bin/claude-switch
only-apps	apps/devterm/web/keytest.html
only-apps	apps/devterm/web/vendor/D2Coding-OFL.txt
only-apps	apps/devterm/web/vendor/d2coding-400.woff2
only-apps	apps/devterm/web/vendor/d2coding-700.woff2
only-apps	apps/feedback/tests/lifecycle.sh
only-apps	apps/feedback/tests/parity.sh
only-apps	apps/fileview/state.sh
only-apps	apps/fileview/static/fileview-manifest.json
only-apps	apps/fileview/tests/lifecycle.sh
only-apps	apps/fileview/tests/parity.sh
only-apps	apps/notepad/render.sh
only-apps	apps/notepad/tests/lifecycle.sh
only-apps	apps/notepad/tests/parity.sh
only-apps	apps/orca/bin/refresh-web-bundle.sh
only-apps	apps/orca/release-pin.sh
only-apps	apps/orca/state.sh
only-apps	apps/orca/tests/lifecycle.sh
only-apps	apps/orca/tests/parity.sh
only-apps	apps/orca/web-bundle/public-scrub.json
only-apps	apps/paseo/patches/codex-strip-ambient-openai-key.mjs
only-apps	apps/paseo/patches/codex-strip-ambient-openai-key.patch
only-apps	apps/paseo/patches/codex-strip-ambient-openai-key.test.mjs
only-apps	apps/paseo/state.sh
only-apps	apps/paseo/tests/lifecycle.sh
only-apps	apps/paseo/tests/parity.sh
only-apps	apps/publish/tests/lifecycle.sh
only-apps	apps/publish/tests/parity.sh
content	apps/code-server/README.md
content	apps/code-server/install.sh
content	apps/dev-monitor/README.md
content	apps/dev-monitor/airlock-app.toml
content	apps/dev-monitor/backend/airlock-dev-monitor.py
content	apps/dev-monitor/backend/devmon_messages.py
content	apps/dev-monitor/backend/devmon_spool.py
content	apps/dev-monitor/backend/devmon_owner.py
content	apps/dev-monitor/backend/devmon_slack.py
content	apps/dev-monitor/backend/devmon_tokens.py
content	apps/dev-monitor/backend/test_devmon.py
content	apps/dev-monitor/frontend/dev-monitor.html
content	apps/dev-monitor/install.sh
content	apps/dev-monitor/render.sh
content	apps/dev-monitor/smoke.sh
content	apps/dev-monitor/migrate-legacy-state.py
content	apps/dev-monitor/test-backend.py
content	apps/dev-monitor/token-freshness.py
content	apps/devterm/README.md
content	apps/devterm/airlock-app.toml
content	apps/devterm/backend/devterm-gate.py
content	apps/devterm/deactivate.sh
content	apps/devterm/install.sh
content	apps/devterm/render.sh
content	apps/devterm/smoke.sh
content	apps/devterm/test-accounts.py
content	apps/devterm/web/app.js
content	apps/devterm/web/index.html
#    2026-09-01: popup.css joins its own siblings above. It was the last devterm web
#    asset still byte-identical to the published copy, so a work-side fix (the panel
#    flavour never reset .tab-pop's 66vh cap, which hid the login button inside the
#    return widget's modal) is what split it. Same batch (1) work-ahead drift as
#    web/app.js and web/secretdrop.js beside it, healed the same way: the mirror
#    catches up on the next publish, not by copying the fix backwards.
content	apps/devterm/web/popup.css
content	apps/feedback/backend/airlock-feedback.py
content	apps/notepad/airlock-app.toml
content	apps/notepad/deactivate.sh
content	apps/notepad/frontend/notepad.html
content	apps/notepad/install.sh
content	apps/orca/airlock-app.toml
content	apps/orca/bin/verify-web-bundle.sh
content	apps/orca/deactivate.sh
content	apps/orca/install.sh
content	apps/orca/render.sh
content	apps/orca/web-bundle/README.md
content	apps/orca/web-bundle/VERSION
# batch 5 (above) also edits two files that exist on BOTH sides, so they are content drift,
# not a new split: patch-web-ui.js gained named-mode dispatch and browse-host/install.sh
# now requests --browse explicitly. Both heal on the next publish sync.
content	apps/paseo/browse-host/bin/patch-web-ui.js
content	apps/paseo/browse-host/install.sh
content	apps/paseo/README.md
content	apps/paseo/airlock-app.toml
content	apps/paseo/install.sh
# 9) 2026-08-23 legacy singleton handover: the private installer now authorizes
#    stale-pid cleanup only after measuring and releasing the actual Paseo owner.
#    The public apps release is explicitly out of scope for this card, so this
#    existing mirrored helper is an expected work-ahead content split until a
#    separately authorized publish sync.
content	apps/paseo/paseo-clear-stale-pid.py
content	apps/paseo/patches/README.md
content	apps/paseo/patches/anchor-manifest.json
content	apps/paseo/render.sh
content	apps/publish/README.md
content	apps/publish/backend/airlock-publish.py
content	apps/publish/frontend/publish.html
content	apps/publish/render.sh
content	apps/publish/test-bundle-plan-state.mjs
content	apps/publish/test-local-target.py
content	apps/code-server/manager/manager.py
content	apps/feedback/README.md
content	apps/feedback/airlock-app.toml
content	apps/orca/README.md
content	apps/paseo/browse-host/README.md
content	apps/paseo/browse-host/smoke.sh
content	apps/paseo/browse-host/src/host.js
content	apps/paseo/browse-host/src/stream-server.js
content	apps/publish/airlock-app.toml
# 7) 2026-08-22 shared state directory: apps/publish/install.sh stopped chmod'ing the
#    SHARED state directory (~/.local/state/airlock) to 0700 on every install. That
#    directory holds the ledger and dev-monitor's spool, and the spool is written by a
#    second uid that has to traverse it — measured on hardware, publish closed it after
#    dev-monitor opened it and the spool writer could not write at all, at RUNTIME, with
#    the install reporting success. Batch 1's shape: work is the live consumer and the
#    public copy is behind until the next publish sync, not a designed split.
content	apps/publish/install.sh
# 12) 2026-08-24 platform-owned credentials P4-P5: devterm's secret test now drives
# the platform CLI through the HTTP relay, and paseo's installer comment names the
# platform pool-record schema it is preserving. These are content changes awaiting the
# next public mirror sync; neither is an intentional long-term implementation fork.
content	apps/paseo/patches/credential-key-preservation.test.mjs
# 11) 2026-08-24 platform-owned credentials P3: the transport boundary is a new
# work-ahead backend module. It invokes the handed-in platform CLI while
# devmon_tokens.py stays a pure (raw JSON, clock) verdict engine. The public mirror
# receives it at the next publish sync; it is not a permanent private-only file.
only-work	apps/dev-monitor/backend/devmon_accounts.py
# 13) 2026-09-01 update detection: this is a dev-monitor package backend helper, not
# a platform-only test. The platform-owned daily unit invokes it from the installed
# dev-monitor package, so it belongs in the next public app mirror. This work tree is
# the live consumer until that sync; recording this exact temporary only-work path
# preserves the gate's refusal of any unrelated private backend module.
only-work	apps/dev-monitor/backend/devmon_updates.py
# 14) 2026-09-10 app-store A: owner home order is a public dev-monitor backend
# helper, but this card ships only the private work source. The public projection is
# a separate, explicitly approved release; keep this work-ahead path narrow until it.
only-work	apps/dev-monitor/backend/devmon_home_order.py
# 15) 2026-09-11 app-store B: the owner app inventory/config writer is likewise a
# public dev-monitor backend helper whose public mirror ships separately. Record only
# this new module; existing backend/install/render edits remain content drift and do
# not widen the set of work-only paths.
only-work	apps/dev-monitor/backend/devmon_apps.py
# 16) 2026-09-12 inbox Hub Phase 3: the caller contract and its live verifier are public
# dev-monitor package files. This source PR does not publish the separate app mirror;
# keep the temporary work-ahead split to these two new paths until that release sync.
only-work	apps/dev-monitor/test-hub-msgstrip-contract.mjs
only-work	apps/dev-monitor/test-inbox-module-contract.mjs
only-work	apps/dev-monitor/verify-inbox-hub-live.py
# 8) 2026-08-22 secret drop double header: the embedded panel drew a second title bar
#    inside the widget's own, because the guard keyed on `close` (which panel.html hands
#    over precisely when embedded) instead of on who owns the window. The fix edits
#    web/secretdrop.js and adds its first test. Batch 1's shape — work is the live
#    consumer and the public copy is behind until the next publish sync, not a designed
#    split. test-secretdrop-js.mjs sits beside test-accounts-js.mjs (batch 1, above),
#    which is only-work for the same reason and is expected to empty the same way.
#    🔴 Both paths are inside the 15-file set the pending public release decision covers
#    (alp-release-scope) — web/secretdrop.js is NOT in that set today, so releasing the
#    recommended 15 files does NOT carry this fix. It needs the next publish sync.
# 9) 2026-08-23 D5 ABI enforcement (apps/ cutover prerequisite): every app package
#    stopped deriving the platform root by climbing "$0/../..", stopped defaulting
#    AIRLOCK_ROOT, and stopped reading platform paths outside install/lib.sh and
#    gate/*. Standing gate: install/check-app-abi.sh (controls in test-app-abi.sh).
#    Batch 1's shape — work is the live consumer of apps/ and the public copy is
#    behind until the next publish sync, not a designed split. This batch is
#    expected to EMPTY at that sync, like batch 3, unlike batches 1-2.
#    The four .in/render/token-timer entries are the same change reaching the two
#    files where @REPO@ conflated "the platform" with "this package": the units now
#    carry @APPDIR@, so nothing bakes <platform>/apps/<id> into systemd any more.
content	apps/code-server/smoke.sh
content	apps/dev-monitor/install-token-timer.sh
content	apps/dev-monitor/systemd/airlock-token-freshness-failed.service.in
content	apps/dev-monitor/systemd/airlock-token-freshness.service.in
content	apps/feedback/install.sh
content	apps/feedback/render.sh
content	apps/feedback/smoke.sh
content	apps/notepad/smoke.sh
content	apps/orca/smoke.sh
content	apps/paseo/smoke.sh
content	apps/publish/smoke.sh

# 4) 2026-08-25: apps/notes — published. The owner ruled every app public
#    ("다 공개앱으로 일단넣어", 2026-08-25), airlock-apps#11 merged (e0a75067),
#    and the eleven only-work paths this batch listed all healed. What remains
#    is the mirror image of batch (2): one test that lives only on the apps
#    side, because the published app carries its own lifecycle test there.
only-apps	apps/notes/tests/lifecycle.sh

# 5) 2026-08-25: markwand -> fileview 개명. **예고한 대로 비워졌다.**
#    개명이 공개본에 실렸고(airlock-apps#13, 42f24f82), 이 batch 가 등록해 둔 서른한 줄
#    중 스물아홉이 healed 가 됐다 — work 에만 있던 fileview 열, 공개본에만 남아 있던
#    markwand 열아홉이다. 그 줄들을 여기서 지운다: 나은 항목을 남겨 두면 이 파일은
#    「아는 갈라짐」이 아니라 「한때 갈라졌던 것」의 목록이 되고, 그 위에서는 kind 가
#    나빠져도 눈에 띄지 않는다.
#    같은 개명이 apps/code-server/airlock-app.toml 의 content 갈라짐도 지웠다(공개본
#    주석이 이제 work 와 같은 이름을 부른다). 그것도 함께 뺀다.
#    남는 둘은 설계된 갈라짐이다 — 공개본의 install.sh 와 viewer.html 은 공개본에만
#    있는 parity 편집 셋(manifest 설치 · 설정 변경 전 fail-closed DB 백업 · linger 경고)을
#    지고 있어 같은 파일이되 내용이 다르다. only-work 가 content 로 바뀐 것은 나빠진
#    것이 아니라 나아진 것이고, 그 새 kind 를 여기 적어 둔다.
content	apps/fileview/install.sh
content	apps/fileview/static/viewer.html
content	apps/learning/airlock-app.toml
content	apps/learning/backend/providers.py
content	apps/learning/render.sh
content	apps/notes/install.sh
content	apps/notes/smoke.sh
# 9) 2026-08-25 paseo ui-state: the cross-device sidebar-order backend and its two
#    offline test files. Batch 1's shape — work is the live consumer and the public
#    copy is behind until the next publish sync, not a designed split. The unit, the
#    gate location and the bundle anchor that use them are edits to files this list
#    already tracks; only these three paths are new.
only-work	apps/paseo/backend/airlock-paseo-uistate.py
only-work	apps/paseo/test-uistate-adapter.mjs
only-work	apps/paseo/test-uistate-backend.py
# 10) 2026-08-27 read-only cron tab: the tab lost its write controls, which took the
#     write surface out of devmon_cron.py. Batch 1's shape again — work is the live
#     consumer and the published copy is behind until the next publish sync, not a
#     designed split. Every other file the change touched (the backend, the frontend,
#     README, smoke, test-backend) is already tracked above; this is the one path
#     that had stayed byte-identical until now.
content	apps/dev-monitor/backend/devmon_cron.py
# 11) 2026-09-01 platform default agent CLI: the action runner stopped having 'claude'
#     written into its argv and now reads the platform's [agent].provider, and learning
#     carries the vendored copy of the platform selection rule (bin/agent_provider.py —
#     a package cannot import from the platform tree, so the copy is the mechanism, and
#     install/test-agent-provider.sh pins it byte-identical). Batch 1's shape again:
#     work is the live consumer and the published copy is behind until the next publish
#     sync, not a designed split. Every other file the change touched inside apps/
#     (airlock-dev-monitor.py, install.sh, render.sh, test_devmon.py, learning's
#     providers.py) is already tracked above; these two paths are the new ones.
content	apps/dev-monitor/backend/action_runner.py
only-work	apps/learning/backend/agent_provider.py
# 12) 2026-09-01 ACCT_OWN: the subscription account panel moved from devterm to the
#     platform (hub/assets/accounts/). Same shape as batch 10 above it in the
#     ownership sense — the work tree deliberately no longer has these two paths, and
#     airlock-apps keeps them until the next publish sync. accounts.js was a content
#     split and panel.html was byte-identical; both become only-apps, and changing a
#     kind is part of an ownership move rather than new drift.
only-apps	apps/devterm/web/accounts.js
only-apps	apps/devterm/web/panel.html
# 14) 2026-09-01 update execution (UPD_EXEC): the wrapper the approved-action runner
#    execs to run bin/airlock-update and capture the before/after airlock-status
#    summary. Same shape and same reason as batch 13's devmon_updates.py directly
#    above — a dev-monitor package backend helper, not a platform-only test — so it
#    belongs in the next public app mirror and this work tree is the live consumer
#    until that sync. Recording this exact temporary only-work path keeps the gate
#    refusing any unrelated private backend module. The rest of the change edits
#    paths already tracked above (airlock-dev-monitor.py, install.sh, test-backend.py).
only-work	apps/dev-monitor/backend/devmon_update_exec.py
# 15) 2026-09-01 learning publish route (#297): the fix that stopped the published
#    "주소 복사" button assembling a :8000 URL edited two files that airlock-apps also
#    carries, so both became content drift — and the entry was not added, which left
#    `main` itself red on this gate. Recorded here, not reverted: the fix is correct
#    and the public copy receives it at the next publish sync, exactly like batch 1.
#    Measured against airlock-apps f9ec7c7 — 1125517 (before #297) rc=0 extra=0 ·
#    efa5c28 (#297) rc=1 extra=2 · 72018db (#293 on top of it) rc=1, the same 2 paths
#    and no new one. So this is #297's split alone; #293 only inherited a red main.
content	apps/learning/backend/airlock-learning.py
content	apps/learning/frontend/learning.html
# 16) 2026-09-01 harness section (HARNESS_PANEL): the wrapper the approved-action runner
#    execs to upgrade the Codex CLI, and the read side of the settings panel's four
#    harness layers. Same shape and same reason as batches 13 and 14 directly above —
#    a dev-monitor package backend helper, not a platform-only test — so it belongs in
#    the next public app mirror and this work tree is the live consumer until that sync.
#    The rest of the change edits paths already tracked above (airlock-dev-monitor.py,
#    devmon_updates.py, devmon_update_exec.py, install.sh, test-backend.py).
only-work	apps/dev-monitor/backend/devmon_harness.py
# 17) 2026-09-02 learning library git sync (#316): the app now commits and pushes the
#    documents it wrote, when the library IS a git repository root and the operator
#    turned it on (`git_sync = "auto"`, default off). The helper is a new package
#    backend file and the ingest worker calls it on its own beat, so this is the same
#    shape as batches 13, 14 and 16 above — an app-side addition that belongs in the
#    next public app mirror, with this work tree as the live consumer until that sync.
#    The rest of the change edits paths already tracked above (airlock-app.toml,
#    airlock-learning.py, install.sh, render.sh).
only-work	apps/learning/backend/git_sync.py
content	apps/learning/backend/ingest_runner.py
# 18) 2026-09-04 fileview home scope (FILEVIEW_HOME): filebrowser stopped serving `/`
#    and now serves the running account's home (`--root %h`), which touched the unit
#    renderer, the manifest's scope comment, the live smoke (its round-trip moved
#    under $HOME and it gained an above-home leak check) and the viewer's client-side
#    path translation. devterm gained one offline suite for the same boundary — the
#    terminal's file-open has to refuse an out-of-home path as `outside_home`.
#    Same shape as batches 1, 13, 14, 16 and 17: work is the live consumer of apps/
#    and the published copy is behind until the next publish sync, not a designed
#    split. apps/fileview/install.sh and static/viewer.html are the designed split
#    already tracked in batch 5, and the change edits both; they keep their kind.
only-work	apps/devterm/test-fileview-scope.py
content	apps/fileview/airlock-app.toml
content	apps/fileview/render.sh
content	apps/fileview/smoke.sh
content	apps/fileview/static/app.js
# 19) 2026-09-05 learning transcript rate-limit (#359): yt-dlp fails the whole run when
#    any requested subtitle language is refused, so asking for `ko,ko-orig,en` in one
#    call let a single 429 on the English fallback discard a Korean transcript that had
#    already arrived — 18 of 118 ingest jobs hit it between 09-02 and 09-05 and every
#    failure read `for 'en'`. The fix asks the video's own language first and keeps `en`
#    as a second round, and backs off on 429 in the first round only. Same shape as
#    batches 1, 13, 14, 16, 17 and 18: work is the live consumer of apps/ and the
#    published copy is behind until the next publish sync, not a designed split.
#    2026-09-05: synced — airlock-apps#16 merged the same fix, so this path healed and
#    its entry is gone. The batch is kept for the trail, not for a live split.
# 20) 2026-09-07 Codex usage sweep: devterm's Codex reading was refreshed only when a
#    panel asked for it, so on a box where nobody opened the Codex section the number
#    stopped moving — one observed gate restart left no state file for 7.5 hours. The
#    gate now sweeps it on the cache TTL, and gained one offline suite for the sweeper's
#    contract (keeps asking · never passes force · survives a raising reading · lets
#    CancelledError through). Same shape as batches 1, 13, 14, 16, 17 and 18: work is
#    the live consumer of apps/ and the published copy is behind until the next publish
#    sync, not a designed split. devterm-gate.py itself is already content drift in
#    batch 2 and keeps its kind.
only-work	apps/devterm/test-codex-usage-sweep.py
# 21) 2026-09-08 Slack published-document unfurls: this is a new public app
#    package, so all eight files belong in airlock-apps at the next mirror sync.
#    Work remains the live consumer until then; this is expected work-ahead drift,
#    not a designed split. Remove the entries as soon as that sync lands.
only-work	apps/slack-unfurl/airlock-app.toml
only-work	apps/slack-unfurl/backend/slack_unfurl.py
only-work	apps/slack-unfurl/backend/socket_mode.py
only-work	apps/slack-unfurl/deactivate.sh
only-work	apps/slack-unfurl/install.sh
only-work	apps/slack-unfurl/render.sh
only-work	apps/slack-unfurl/smoke.sh
only-work	apps/slack-unfurl/test-slack-unfurl.py
# 22) 2026-09-08 코어 라이선스 표기 변경(AGPL-3.0): 저작권자가 Apache-2.0 → AGPL-3.0 으로
#    확정해(#392) 트리의 `SPDX-License-Identifier` 를 전부 `AGPL-3.0-only` 로 맞췄다.
#    browse-host 는 mirrored app 이라 그 헤더 한 줄이 여기 열일곱 경로를 content 로 가른다.
#    batch 1 과 같은 모양이다 — work 가 apps/ 의 live consumer 이고 공개본이 뒤에 있다.
#    설계된 갈라짐이 **아니다**: 다음 publish sync 에서 airlock-apps 가 같은 헤더를 받으면
#    열일곱 줄 전부 healed 가 되어야 하고, 그때 이 배치를 지운다.
#    🔴 그때까지 airlock-apps 의 이 파일들은 헤더에 Apache-2.0 을 달고 있다. 정본은 저장소
#    루트의 LICENSE 이고 그쪽은 아직 옛 값이므로, 공개 미러의 라이선스 표기는 sync 전까지
#    실제와 어긋난다. 이 갈라짐을 오래 두지 말 것 — 라이선스는 늦게 맞춰도 되는 종류의
#    드리프트가 아니다.
#    browse-host 의 host.js·stream-server.js·smoke.sh·install.sh·README.md·
#    bin/patch-web-ui.js 는 이미 위에서 content 로 잡혀 있어 kind 가 그대로다.
content	apps/paseo/browse-host/bin/paseo-browse-host.js
content	apps/paseo/browse-host/package.json
content	apps/paseo/browse-host/src/commands.js
content	apps/paseo/browse-host/src/executor.js
content	apps/paseo/browse-host/src/security.js
content	apps/paseo/browse-host/test/broker.integration.mjs
content	apps/paseo/browse-host/test/companion.e2e.cjs
content	apps/paseo/browse-host/test/companion.unit.cjs
content	apps/paseo/browse-host/test/executor.ensuretab.cjs
content	apps/paseo/browse-host/test/executor.roundtrip.cjs
content	apps/paseo/browse-host/test/logs.unit.cjs
content	apps/paseo/browse-host/test/roster.integration.cjs
content	apps/paseo/browse-host/test/roster.unit.cjs
content	apps/paseo/browse-host/test/security.policy.cjs
content	apps/paseo/browse-host/test/smoke.cjs
content	apps/paseo/browse-host/test/stream.roundtrip.cjs
content	apps/paseo/browse-host/web/browse-view-client.js

# 2026-09-10 dev-monitor Phase 1: app-only secret contract regression suite.
only-work	apps/dev-monitor/test-secrets.py

# Phase 1 review: shared boolean secret check and public smoke regressions.
only-work	apps/dev-monitor/secret-check.sh
only-work	apps/dev-monitor/test-secret-regressions.py
only-work	apps/dev-monitor/backend/devmon_secret_names.py
only-work	apps/dev-monitor/backend/devmon_secrets.py
only-work	apps/dev-monitor/check-secrets.py
only-work	apps/dev-monitor/test-secret-resolution.py
only-work	apps/dev-monitor/backend/devmon_secret_file.py
# 2026-09-10 dev-monitor Phase 2: daily producer, units, and local E2E oracle.
only-work	apps/dev-monitor/heartbeat.py
only-work	apps/dev-monitor/backend/devmon_heartbeat.py
only-work	apps/dev-monitor/test-heartbeat.py
only-work	apps/dev-monitor/systemd/airlock-devmon-heartbeat.service.in
only-work	apps/dev-monitor/systemd/airlock-devmon-heartbeat.timer.in
only-work	apps/dev-monitor/test-frontend-contract.mjs
only-work	apps/dev-monitor/test-message-contract.py
content	apps/dev-monitor/examples/emit_message.py
content	apps/dev-monitor/token-freshness-alarm.sh
only-work	apps/dev-monitor/test-run-contract.py

# Phase 6: app-only loop, offline row mapping, and decisive endstate oracles.
only-work	apps/dev-monitor/backend/devmon_loop.py
only-work	apps/dev-monitor/backend/devmon_migrate.py
only-work	apps/dev-monitor/test-loop-contract.py
only-work	apps/dev-monitor/test-endstate-rehearsal.py

# 2026-09-12 dev-monitor Phase 4a: Cron-tab cards are a work-ahead production
# path. The scan and its fixture exercise the public app's existing Cron data
# differently, while the before/after verifier is an operator-only live gate;
# all three await the next airlock-apps projection sync.
content	apps/dev-monitor/backend/devmon_cron_scan.py
content	apps/dev-monitor/backend/test_devmon_cron_scan.py
only-work	apps/dev-monitor/verify-cron-handoff-live.py

# 2026-09-11 company catalog: public dev-monitor module and its offline contract tests
# are work-ahead until the next airlock-apps projection sync.

only-work	apps/dev-monitor/backend/devmon_company_catalog.py
only-work	apps/dev-monitor/backend/test_devmon_company_catalog.py

# 2026-09-11 guarded Paseo 0.2.5: Airlock now carries the reviewed six-package
# set as its reproducible default install input. The public platform bundle and the
# three live dev boxes consume this work tree directly; airlock-apps remains a
# separately pinned projection and receives the same immutable directory at its next
# app release rather than a partial sync onto its older Paseo installer.
only-work	apps/paseo/vendor/guarded-0.2.5/INSTALLED_SHA256SUMS
only-work	apps/paseo/vendor/guarded-0.2.5/LICENSE
only-work	apps/paseo/vendor/guarded-0.2.5/README.md
only-work	apps/paseo/vendor/guarded-0.2.5/SHA256SUMS
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-cli-0.2.5.tgz
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-client-0.2.5.tgz
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-highlight-0.2.5.tgz
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-protocol-0.2.5.tgz
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-relay-0.2.5.tgz
only-work	apps/paseo/vendor/guarded-0.2.5/getpaseo-server-0.2.5.tgz

# 2026-09-12 적재 스킬 문체 규칙: 요약 프롬프트를 원칙 중심으로 다시 쓰면서 문체 규칙을
# 신설했고(개요만 불릿, 그 밖의 절은 문단), 자막 경고를 본문에서 빼 프론트매터로 옮겼다.
# work 가 앞서 있고 공개 미러는 다음 앱 릴리스에서 같은 파일을 통째로 받는다 — 이 레포가
# 저작 원본이다.
content	apps/learning/skill/SKILL.md

# 2026-09-12 Learning 자막 요청 축소: transcript.py 는 메타 목록에서 트랙 하나만 골라
# 요청하고 닫힌 실패 sentinel을 낸다. 공개 앱에 속하는 기존 파일이며, 공개 미러가 다음
# 동기화에서 같은 변경을 받을 때까지 work 쪽이 앞선 content drift로 기록한다.
content	apps/learning/skill/transcript.py

# 2026-09-12 Learning 읽기 셸 계약: 비공개 저작 원본의 프런트 계약 시험이 `/read/`
# 셸·Markdown 복사·설정 저장·공유 단계를 새로 고정했다. 이 시험도 앱과 함께 공개
# 미러가 다음 릴리스에서 받는 임시 content drift다.
content	apps/learning/test-frontend-contract.mjs

# 2026-09-12 Learning 타임스탬프 링크: 결정적 HTML 변환기는 서버 옆에 설치되는 새 공개 앱
# 백엔드 모듈이다. 공개 미러가 다음 명시적 앱 릴리스에서 받을 때까지 work 쪽에만 있는 경로로
# 좁게 기록한다. 기존 Learning 파일은 위의 content drift 범위 안에 남는다.
only-work	apps/learning/backend/timestamp_links.py

# 2026-09-12 Learning 적재 흐름 v3: 저장 헬퍼가 새 문서의 파일명 형식
# (`<날짜>--<슬러그>--<video_id>.md`)을 검사한다. 공개 앱에 속하는 기존 파일이며, 공개
# 미러가 다음 앱 릴리스에서 같은 변경을 받을 때까지 work 쪽이 앞선 content drift로 기록한다.
content	apps/learning/backend/save_document.py

# 2026-09-13 platform secret drop: the drop's UI and HTTP API moved from devterm to the
# platform (hub/assets/accounts/secretdrop.js, bin/airlock-accounts-api), and its tests
# with them (install/test-secret-drop.py, install/test-secret-drop-ui.mjs). The public
# mirror still carries devterm's old copies until the next app release, which is when
# these three become healed deletions — a designed move, not new drift. ui.js is content
# drift for one reason only: its header no longer claims consumers it lost.
# docs/tasks/active/platform-secret-drop.md.
only-apps	apps/devterm/test-secrets.py
only-apps	apps/devterm/web/secretdrop.js
content	apps/devterm/web/ui.js

# 2026-09-13 weekly-docs run template: these two files are an intentional permanent
# work-only overlay. Their fixed prompt and exact contract name private company paths,
# while the public dev-monitor keeps only the generic receiver and no-overlay tests.
# `install/public-manifest.sh` independently excludes both exact paths from export.
only-work	apps/dev-monitor/backend/devmon_weekly_docs.py
only-work	apps/dev-monitor/backend/test_devmon_weekly_docs.py
