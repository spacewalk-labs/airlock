프로덕션 빌드를 실행하고 결과를 보고한다.

1. `node_modules/` 존재 여부 확인. 없으면 `pnpm install` 실행.
2. `pnpm build`를 실행한다.
3. 빌드 성공 시:
   - 번들 크기를 요약한다.
   - 빌드 경고가 있으면 알려준다.
4. 빌드 실패 시:
   - 에러 메시지를 정확히 분석한다.
   - 타입 에러, 임포트 에러, 빌드 설정 문제를 구분한다.
   - 구체적인 수정 방안을 제안한다.
