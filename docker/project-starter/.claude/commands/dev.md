개발 서버를 시작한다.

1. `node_modules/` 존재 여부 확인. 없으면 `pnpm install` 실행.
2. 포트 3000이 이미 사용 중인지 `lsof -i :3000`으로 확인.
   - 사용 중이면 사용자에게 알리고 종료할지 물어본다.
3. `pnpm dev`를 실행한다.
4. 서버 시작 후 http://localhost:3000 접속 URL을 알려준다.
5. 에러 발생 시 에러 메시지를 분석하고 해결 방안을 제안한다.
