# 환경변수 설정
# .env.example → .env.local로 복사 후 값을 채우세요
# .env.local은 git에 커밋되지 않습니다 (훅 + gitleaks 가 커밋을 차단합니다)

# Database
# DATABASE_URL=

# Auth (Auth.js v5 / NextAuth 5 — 예전 NEXTAUTH_* 에서 AUTH_* 로 변경됨)
# AUTH_SECRET=
# AUTH_URL=http://localhost:3000

# External APIs
# SLACK_WEBHOOK_URL=
